<!DOCTYPE html>
<html lang="en">

<head>
	<% include partials/head %>
</head>

<body>
	<% include partials/navbar %>
</br>
	<div class="container">
			<div class="col-sm-6">
				<div class="text-center"><img src="/images/loader.gif" id="loader" style="display:none" /></div>
			</div>
			<div class="col-sm-6">
				<button id="sync-btn" data-url="/admin/loadUsers" type="submit" class="btn btn-primary">Sync Users</button>	
				<div></br></div>
			</div>
		<table class="table">
			<tr>
				<th>Name</th>
				<th>Username</th>
				<th>Instance Id</th>
				<th>Instance IP</th>
				<th>Instance Status</th>
				<th>Create Instance</th>
				<th>Release Instance</th>
			</tr>

			<% for (var i = 0; i < vdiusers.length; i++) { %>
			<% if (vdiusers[i].instanceStatus == 'Running') { %>
			<tr class="info">
				<% } else { %>
			<tr>
				<% } %>
				<td><%= vdiusers[i].name %></td>
				<td class="username"><%= vdiusers[i].username %></td>
				<td class="instanceId"><%= vdiusers[i].instanceId %></td>
				<td class="instanceIP"><%= vdiusers[i].instanceIP %></td>
				<td class="status"><%= vdiusers[i].instanceStatus %></td>
				<td>
					<button id='create-btn-<%= i %>' type="submit" class="btn btn-danger btn-sm create-btn">Create Instance</button>
				</td>
				<td>
					<button id='release-btn-<%= i %>' data-url="/admin/releaseInstance" type="submit" class="btn btn-danger btn-sm release-btn">Release Instance</button>
				</td>
			</tr>
			<% } %>
		</table>
	</div>
</body>
<script src="/javascripts/navbar.js"></script>
<script src="/javascripts/admin-dashboard.js"></script>

</html>